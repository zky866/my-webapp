<P>
    <a href="{% url "main:index"%}">主页</a>
    <a href="{% url "urls:urls"%}">网址大全</a>
    <a href="{% url "notebook:notebook"%}">笔记本</a>
    {% if user.is_authenticated %}
      你好，{{user.username}}!
      <form action="{% url 'users:logout' %}" method="post" style="display:inline;">
        {% csrf_token %}
        <button type="submit" style="border:none;background:none;cursor:pointer;">退出登录</button>
      </form>
    {% else %}
     <a href="{% url "users:login"%}">登录</a>
    {% endif %}
</P>

{% block content %}{% endblock content %}